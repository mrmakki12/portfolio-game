// import platform image here

export class Platform {
    constructor(x, y) {
        // set position
        this.position = {
            x,
            y,
        }

        // platform image
        this.image = ''
    }

    // draw it
    // ctx == canvas context
    draw(ctx) {

    }
}