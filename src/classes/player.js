// import avatar here

export class Player {
    constructor() {
        // keep track of position
        this.position = {
            x: 100,
            y: 500
        }

        this.avatar = ''
    }

    // draw player
    // ctx == canvas context
    draw(ctx) {
        // probably have to flip image when player goes left

    }

    // update position
    // ctx == canvas context
    update(ctx) {

    }
}
