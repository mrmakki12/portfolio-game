export class SocialLink {
    // img == social logo, link == website link
    constructor(img, link) {
        this.img = img
        this.link = link
    }

    // draw it
    // ctx == canvas context
    draw(ctx) {

    }
}